server {
    listen 80;
    server_name helpsmp.ru www.helpsmp.ru;
    
    # Редирект на HTTPS (будет активен после настройки SSL)
    # return 301 https://$server_name$request_uri;
    
    # Временно для HTTP (до настройки SSL)
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
    
    # Статические файлы
    location /_nuxt/ {
        alias /var/www/helpsmp.ru/_nuxt/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Логи
    access_log /var/log/nginx/helpsmp.ru.access.log;
    error_log /var/log/nginx/helpsmp.ru.error.log;
}

# HTTPS конфигурация (активируется после настройки SSL)
# server {
#     listen 443 ssl http2;
#     server_name helpsmp.ru www.helpsmp.ru;
#     
#     ssl_certificate /etc/letsencrypt/live/helpsmp.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/helpsmp.ru/privkey.pem;
#     
#     # SSL настройки
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 86400;
#     }
#     
#     location /_nuxt/ {
#         alias /var/www/helpsmp.ru/_nuxt/;
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
# }

